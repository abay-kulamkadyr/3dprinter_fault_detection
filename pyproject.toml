[tool.autopep8]
# Enable aggressive formatting (optional)
aggressive = 2
# Only include .py files
recursive = true
exclude = ["*.md", "*.json", "*.yml", "*.yaml"]
max_line_length = 100
in-place = true

